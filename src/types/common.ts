export const enum SortDirection {
  Asc = "asc",
  Dsc = "desc",
}

export const enum StatusType {
  Active = 1,
  Disable = 0,
  Inactive = 2,
}
export enum SortOrder {
  Asc = 0,
  Desc = 1,
}
